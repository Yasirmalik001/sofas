{{ 'template-collection.css' | asset_url | stylesheet_tag }}
{{ 'component-card.css' | asset_url | stylesheet_tag }}
{{ 'component-price.css' | asset_url | stylesheet_tag }}
{{ 'section-collection-tabs.css' | asset_url | stylesheet_tag }}
{% if section.settings.image_shape == 'blob' %}
  {{ 'mask-blobs.css' | asset_url | stylesheet_tag }}
{%- endif -%}

{%- unless section.settings.quick_add == 'none' -%}
  {{ 'quick-add.css' | asset_url | stylesheet_tag }}
{%- endunless -%}

{%- if section.settings.quick_add == 'standard' -%}
  <script src="{{ 'quick-add.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

{%- if section.settings.quick_add == 'bulk' -%}
  <script src="{{ 'quick-add-bulk.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'quantity-popover.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'price-per-item.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'quick-order-list.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="section-{{ section.id }}-padding gradient color-{{ section.settings.color_scheme }}">
  {%- paginate collection.products by section.settings.products_per_page -%}
    {% comment %} Sort is the first tabbable element when filter type is vertical {% endcomment %}
    {%- if section.settings.enable_sorting and section.settings.filter_type == 'vertical' -%}
      <facet-filters-form class="facets facets-vertical-sort page-width small-hide">
        <form class="facets-vertical-form" id="FacetSortForm">
          <div class="facet-filters sorting caption">
            <div class="facet-filters__field">
              <h2 class="facet-filters__label caption-large text-body">
                <label for="SortBy">{{ 'products.facets.sort_by_label' | t }}</label>
              </h2>
              <div class="select">
                {%- assign sort_by = collection.sort_by | default: collection.default_sort_by -%}
                <select
                  name="sort_by"
                  class="facet-filters__sort select__select caption-large"
                  id="SortBy"
                  aria-describedby="a11y-refresh-page-message"
                >
                  {%- for option in collection.sort_options -%}
                    <option
                      value="{{ option.value | escape }}"
                      {% if option.value == sort_by %}
                        selected="selected"
                      {% endif %}
                    >
                      {{ option.name | escape }}
                    </option>
                  {%- endfor -%}
                </select>
                <span class="svg-wrapper">
                  {{- 'icon-caret.svg' | inline_asset_content -}}
                </span>
              </div>
            </div>
          </div>

          <div class="product-count-vertical light" role="status">
            <h2 class="product-count__text text-body">
              <span id="ProductCountDesktop">
                {%- if collection.results_count -%}
                  {{
                    'templates.search.results_with_count'
                    | t: terms: collection.terms, count: collection.results_count
                  }}
                {%- elsif collection.products_count == collection.all_products_count -%}
                  {{ 'products.facets.product_count_simple' | t: count: collection.products_count }}
                {%- else -%}
                  {{
                    'products.facets.product_count'
                    | t: product_count: collection.products_count, count: collection.all_products_count
                  }}
                {%- endif -%}
              </span>
            </h2>
            {%- render 'loading-spinner' -%}
          </div>
        </form>
      </facet-filters-form>
    {%- endif -%}

    <div class="{% if section.settings.filter_type == 'vertical' %} facets-vertical page-width{% endif %}">
      {{ 'component-facets.css' | asset_url | stylesheet_tag }}
      <script src="{{ 'facets.js' | asset_url }}" defer="defer"></script>
      <style>
      .collection-toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-top: 1px solid rgba(var(--color-foreground), 0.08);
    flex-wrap: wrap;
    flex-direction: row-reverse;
    gap: 15px;
}
        .collection-toolbar__nav {
          flex: 1;
          overflow-x: auto;
          -webkit-overflow-scrolling: touch;
          scrollbar-width: none; /* Hide scrollbar in Firefox */
          width: 100%;
        }
        .collection-toolbar__nav::-webkit-scrollbar {
          display: none; /* Hide scrollbar in Webkit */
        }
        .cn-list {
          display: flex;
          gap: 10px;
          list-style: none;
          padding: 0;
          margin: 0;
          min-width: max-content;
        }
        .cn-item {
          display: flex;
          align-items: center;
          gap: 10px;
          padding: 8px 16px;
          border-radius: 30px;
          text-decoration: none !important;
          color: rgb(var(--color-foreground));
          font-weight: 500;
          font-size: 1.4rem;
          background: transparent;
          transition: background-color 0.3s ease, opacity 0.3s ease;
        }
        .cn-item:hover {
          opacity: 0.7;
          text-decoration: none !important;
        }
        .cn-item.active {
          background-color: #efe6d8;
          opacity: 1;
        }
        .cn-item img {
          height: 24px;
          width: auto;
          object-fit: contain;
        }
        .collection-toolbar__controls {
          display: flex;
          align-items: center;
          gap: 20px;
        }
        .grid-view-toggles {
          display: flex;
          align-items: center;
          gap: 8px;
        }
        .grid-view-toggle {
          background: transparent;
          border: none;
          padding: 6px;
          border-radius: 6px;
          cursor: pointer;
          color: #d4d4d4;
          display: flex;
          align-items: center;
          justify-content: center;
          transition: color 0.35s cubic-bezier(0.16, 1, 0.3, 1), background-color 0.35s cubic-bezier(0.16, 1, 0.3, 1), transform 0.2s ease;
        }
        .grid-view-toggle svg rect {
          transition: fill 0.35s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .grid-view-toggle:hover:not(.active) {
          color: #b8b8b8;
        }
        .grid-view-toggle.active {
          color: #2b2b2b;
        }
        .grid-view-toggle:active {
          transform: scale(0.94);
        }
        @media screen and (min-width: 990px) {
          #product-grid.product-grid .grid__item {
            transition:
              width 0.45s cubic-bezier(0.16, 1, 0.3, 1),
              max-width 0.45s cubic-bezier(0.16, 1, 0.3, 1);
          }
          #product-grid.product-grid.is-grid-transitioning .card-product-tabs {
            transition: transform 0.45s cubic-bezier(0.16, 1, 0.3, 1);
          }
        }
        @media screen and (max-width: 768px) {
           .collection-toolbar {
             flex-direction: column;
             align-items: flex-start;
             gap: 0px;
           }
           .collection-toolbar__controls {
             width: 100%;
             justify-content: end;
           }
           .collection-toolbar__nav {
             padding-bottom: 10px;
             padding-top: 10px;
           }
        }
      </style>

      <div class="collection-toolbar page-width">
  

        <div class="collection-toolbar__controls">
          <div class="grid-view-toggles small-hide">
            <button type="button" class="grid-view-toggle js-grid-toggle" data-col="3" aria-label="3 columns">
              <svg xmlns="http://www.w3.org/2000/svg" width="26" height="22" viewBox="0 0 26 22" fill="none" aria-hidden="true">
            <rect width="8" height="22" fill="currentColor"/>
            <rect x="9" width="8" height="22" fill="currentColor"/>
            <rect x="18" width="8" height="22" fill="currentColor"/>
            </svg>
            </button>
            <button type="button" class="grid-view-toggle js-grid-toggle active" data-col="4" aria-label="4 columns">
              <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 22 22" fill="none" aria-hidden="true">
            <rect width="4.4" height="4.4" fill="currentColor"/>
            <rect x="5.86621" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="11.7334" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="17.5996" width="4.4" height="4.4" fill="currentColor"/>
            <rect y="5.86667" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="5.86621" y="5.86667" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="11.7334" y="5.86667" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="17.5996" y="5.86667" width="4.4" height="4.4" fill="currentColor"/>
            <rect y="11.7333" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="5.86621" y="11.7333" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="11.7334" y="11.7333" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="17.5996" y="11.7333" width="4.4" height="4.4" fill="currentColor"/>
            <rect y="17.6" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="5.86621" y="17.6" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="11.7334" y="17.6" width="4.4" height="4.4" fill="currentColor"/>
            <rect x="17.5996" y="17.6" width="4.4" height="4.4" fill="currentColor"/>
            </svg>
            </button>
          </div>

          {%- if section.settings.enable_filtering or section.settings.enable_sorting -%}
            <aside
              aria-labelledby="verticalTitle"
              class="facets-wrapper{% unless section.settings.enable_filtering %} facets-wrapper--no-filters{% endunless %}"
              id="main-collection-filters"
              data-id="{{ section.id }}"
            >
           {% render 'facet-exercer',
            enable_filtering: section.settings.enable_filtering,
            enable_sorting: section.settings.enable_sorting,
            filter_type: section.settings.filter_type
          %}
            </aside>
          {%- endif -%}
        </div>
              <div class="collection-toolbar__nav">
          <ul class="cn-list">
            {%- for block in section.blocks -%}
              {%- if block.type == 'collection_item' -%}
                {%- liquid
                  assign is_active = false
                  
                  if collection.handle != blank and block.settings.collection_ref != blank
                    if collection.handle == block.settings.collection_ref.handle
                      assign is_active = true
                    endif
                  endif

                  if request.path == block.settings.custom_url
                     assign is_active = true
                  endif
                  
                  assign image = block.settings.custom_image
                  if image == blank and block.settings.collection_ref != blank
                     assign image = block.settings.collection_ref.featured_image
                  endif
                  
                  assign link_url = block.settings.custom_url
                  if link_url == blank and block.settings.collection_ref != blank
                     assign link_url = block.settings.collection_ref.url
                  endif
                -%}

                <li>
                  <a href="{{ link_url }}" class="cn-item {% if is_active %}active{% endif %}" {{ block.shopify_attributes }}>
                    {%- if image != blank -%}
                      <img src="{{ image | image_url: height: 48 }}" alt="{{ block.settings.title | escape }}" loading="lazy">
                    {%- endif -%}
                    <span class="cn-title">
                      {%- if block.settings.title != blank -%}
                        {{ block.settings.title }}
                      {%- else -%}
                        {{ block.settings.collection_ref.title }}
                      {%- endif -%}
                    </span>
                  </a>
                </li>
              {%- endif -%}
            {%- endfor -%}
          </ul>
        </div>
      </div>

      <div
        class="product-grid-container{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
        id="ProductGridContainer"
        {% if settings.animations_reveal_on_scroll %}
          data-cascade
        {% endif %}
      >
        
        {%- if collection.products.size == 0 -%}
          <div class="collection collection--empty page-width" id="product-grid" data-id="{{ section.id }}">
            <div class="loading-overlay gradient"></div>
            <div class="title-wrapper center">
              <h2 class="title title--primary">
                {{ 'sections.collection_template.empty' | t -}}
                <br>
                {{
                  'sections.collection_template.use_fewer_filters_html'
                  | t: link: collection.url, class: 'underlined-link link'
                }}
              </h2>
            </div>
          </div>
        {%- else -%}
          <div
            class="collection{% if section.settings.filter_type != 'vertical' %} page-width{% endif %}"
          >
            <div class="loading-overlay gradient"></div>
            <ul
              id="product-grid"
              data-id="{{ section.id }}"
              class="
                grid product-grid grid--{{ section.settings.columns_mobile }}-col-tablet-down
                grid--{{ section.settings.columns_desktop }}-col-desktop
                {% if section.settings.quick_add == 'bulk' %} collection-quick-add-bulk{% endif %}
              "
            >
              {% assign skip_card_product_styles = false %}
              {%- for product in collection.products -%}
                {% assign lazy_load = false %}
                {%- if forloop.index > 2 -%}
                  {%- assign lazy_load = true -%}
                {%- endif -%}
            <li
                  class="grid__item  collection-grid-item{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
                  {% if settings.animations_reveal_on_scroll %}
                    data-cascade
                    style="--animation-order: {{ forloop.index }};"
                  {% endif %}
                >
                  {% render 'card-product-tabs',
                            card_product: product,
                            section_id: section.id,
                            media_aspect_ratio: section.settings.image_ratio,
                            show_secondary_image: section.settings.show_secondary_image,
                            show_rating: section.settings.show_rating,
                            show_swatches: section.settings.show_swatches,
                            swatch_option: section.settings.swatch_option_name,
                            swatch_limit: 6,
                            show_delivery: section.settings.show_delivery,
                            delivery_text: section.settings.delivery_text,
                            lazy_load: lazy_load,
                            badge_position: section.settings.badge_position,
                            quick_add: section.settings.quick_add
                          %}
                </li>
                {% assign index = forloop.index %}
                {%- assign skip_card_product_styles = true -%}
                {% for block in section.blocks %}
                  {% if block.settings.insert_after == index %}
                    {% case block.type %}
                      {% when 'bmsm_promo' %}
                        {% render 'card-countdown-promo', block: block %}
                        {% when 'feature_promo' %}
        {% render 'card-feature-promo', block: block %}
                    {% endcase %}
                  {% endif %}
                {% endfor %}
              {%- endfor -%}
            </ul>

            {% if section.settings.infinite == 'infinite' %}
              <input type="hidden" id="total-pages" value="{{ paginate.pages }}">
              {% if section.settings.infinite_progressbar %}
                <!-- Load more progressbar -->
                <input type="hidden" id="collection-total-count" value="{{ collection.all_products_count }}">
                <div class="collection-progress-wrapper">
                  <p id="progress-text">Showing items...</p>
                  <div class="progress-bar-bg">
                    <div id="progress-bar-fill" style="width: 0%;"> </div>
                  </div>
                </div>
              {% endif %}
              <!-- Load more status -->
              {% if section.settings.infinite_btn %}
                <!-- Load more button -->
                <div class="load-more-container" style="text-align: center; margin: 20px 0;">
                  <button class="view-more-button button button--secondary">View More</button>
                </div>
              {% endif %}
              <div class="page-load-status">
                <div class="infinite-scroll-request">
                  <svg aria-hidden="true" focusable="false" role="presentation" class="spinner" viewBox="0 0 66 66" xmlns="http://www.w3.org/2000/svg">
                    <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
                  </svg>
                </div>
                <p class="infinite-scroll-last">All products loaded</p>
                <p class="infinite-scroll-error">No more pages to load</p>
              </div>
            {% endif %}

            {%- if paginate.pages > 1 -%}
              {% render 'pagination', paginate: paginate, anchor: '' %}
            {%- endif -%}
          </div>
        {%- endif -%}
      </div>
    </div>
  {%- endpaginate -%}
  {% if section.settings.image_shape == 'arch' %}
    {{ 'mask-arch.svg' | inline_asset_content }}
  {%- endif -%}
</div>

{% if section.settings.infinite == 'infinite' %}
<script src="{{ 'infinite-scroll.pkgd.min.js' | asset_url }}" defer="defer"></script>
<script>
  document.addEventListener("DOMContentLoaded", function() {
    var elem = document.querySelector('#product-grid');
    if (elem) {
      var params = new URLSearchParams(window.location.search);
      var startPage = parseInt(params.get('page')) || 1;
      var totalPagesInput = document.getElementById('total-pages');
      var totalPages = totalPagesInput ? parseInt(totalPagesInput.value) : 1;

      var infScroll = new InfiniteScroll( elem, {
        path: function() {
          var nextPage = startPage + this.loadCount + 1;
          if (nextPage > totalPages) {
            return undefined;
          }
          return window.location.pathname + '?page=' + nextPage;
        },
        append: '.grid__item',
        hideNav: '.pagination',
        status: '.page-load-status',
        {% if section.settings.infinite_btn %}
          button: '.view-more-button', 
          scrollThreshold: false
        {% endif %}
      });

      // For load more progressbar:
      {% if section.settings.infinite_progressbar %}
      var totalItems = parseInt(document.getElementById('collection-total-count').value);
      function updateProgress() {
          var currentItems = document.querySelectorAll('.grid__item').length;
          // Ensure we don't show more than 100%
          var percent = Math.min((currentItems / totalItems) * 100, 100);
          var bar = document.querySelector('#progress-bar-fill');
          if(bar) bar.style.width = percent + '%';
          var text = document.querySelector('#progress-text');
          if(text) text.innerText = 'Showing ' + currentItems + ' of ' + totalItems + ' products';
        }
        updateProgress();
      {% endif %}

        // Load more button functions
      {% if section.settings.infinite_btn %}
        infScroll.on( 'request', function() {
          var btn = document.querySelector('.view-more-button');
          if(btn) btn.style.display = 'none';
        });
        
        infScroll.on( 'last', function() {
          var btn = document.querySelector('.view-more-button');
          if(btn) btn.style.display = 'none';
        });
      {% endif %}

        infScroll.on( 'append', function() {
          // Check if we reached the end after appending to hide the button permanently
          if (this.pageIndex < totalPages) {
             {% if section.settings.infinite_btn %}
               var btn = document.querySelector('.view-more-button');
               if(btn) btn.style.display = 'inline-block';
             {% endif %}
          }

          {% if section.settings.infinite_progressbar %}
            updateProgress();
          {% endif %}
      });
    }
  });
</script>
<style>
.page-load-status {
  text-align: center;
  margin-top: 30px;
  margin-bottom: 30px;
}
.infinite-scroll-request,
.infinite-scroll-last,
.infinite-scroll-error {
  display: none;
}
.page-load-status .spinner {
  width: 20px;
  height: 20px;
  animation: rotate 1.4s linear infinite;
}
.infinite-scroll-request .view-more-button {
  display: none; 
}
.page-load-status .spinner .path {
  stroke: #000;
  stroke-dasharray: 187;
  stroke-dashoffset: 0;
  transform-origin: center;
  animation: dash 1.4s ease-in-out infinite;
}
@keyframes rotate {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(270deg); }
}
@keyframes dash {
  0% { stroke-dashoffset: 187; }
  50% { stroke-dashoffset: 46; transform: rotate(135deg); }
  100% { stroke-dashoffset: 187; transform: rotate(450deg); }
}

{% if section.settings.infinite_progressbar %}
 .collection-progress-wrapper {
  max-width: 400px;
  margin: 20px auto;
  text-align: center;
}
.progress-bar-bg {
  width: 100%;
  height: 6px;
  background-color: #e0e0e0;
  border-radius: 3px;
  overflow: hidden;
  margin-top: 10px;
}
#progress-bar-fill {
  height: 100%;
  background-color: #2B2B2B;
  transition: width 0.3s ease;
}
{% endif %}
</style>

{% endif %}

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const gridToggles = document.querySelectorAll('.js-grid-toggle');
    const productGrid = document.getElementById('product-grid');
    const storageKey = 'collection-grid-columns';

    function setGridColumns(columns, toggle) {
      if (!productGrid || !columns) return;

      productGrid.classList.add('is-grid-transitioning');

      gridToggles.forEach(function (btn) {
        btn.classList.toggle('active', btn === toggle);
      });

      Array.from(productGrid.classList).forEach(function (className) {
        if (className.startsWith('grid--') && className.endsWith('-col-desktop')) {
          productGrid.classList.remove(className);
        }
      });

      productGrid.classList.add('grid--' + columns + '-col-desktop');

      try {
        localStorage.setItem(storageKey, columns);
      } catch (e) {
        /* noop */
      }

      window.setTimeout(function () {
        productGrid.classList.remove('is-grid-transitioning');
      }, 480);
    }

    if (gridToggles.length && productGrid) {
      var savedColumns = null;
      try {
        savedColumns = localStorage.getItem(storageKey);
      } catch (e) {
        /* noop */
      }

      if (savedColumns) {
        var savedToggle = document.querySelector('.js-grid-toggle[data-col="' + savedColumns + '"]');
        if (savedToggle) {
          setGridColumns(savedColumns, savedToggle);
        }
      }

      gridToggles.forEach(function (toggle) {
        toggle.addEventListener('click', function () {
          if (this.classList.contains('active')) return;
          setGridColumns(this.getAttribute('data-col'), this);
        });
      });
    }
  });
</script>
<script src="{{ 'collection-tabs.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'WI_quickAdd.js' | asset_url }}" defer="defer"></script>
{% schema %}
{
  "name": "t:sections.main-collection-product-grid.name",
  "class": "section",
  "settings": [
    {
      "type": "range",
      "id": "products_per_page",
      "min": 8,
      "max": 36,
      "step": 4,
      "default": 16,
      "label": "t:sections.main-collection-product-grid.settings.products_per_page.label"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 6,
      "step": 1,
      "default": 4,
      "label": "t:sections.main-collection-product-grid.settings.columns_desktop.label"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "default": "2",
      "label": "t:sections.main-collection-product-grid.settings.columns_mobile.label",
      "options": [
        {
          "value": "1",
          "label": "t:sections.main-collection-product-grid.settings.columns_mobile.options__1.label"
        },
        {
          "value": "2",
          "label": "t:sections.main-collection-product-grid.settings.columns_mobile.options__2.label"
        }
      ]
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "info": "t:sections.all.colors.has_cards_info",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Infinite Scrolling"
    },
    {
      "type": "select",
      "id": "infinite",
      "options": [
        {
          "label": "Pagination",
          "value": "pagination"
        },
        {
          "label": "Infinite",
          "value": "infinite"
        }
      ],
      "label": "Select",
      "default": "pagination"
    },
    {
      "type": "checkbox",
      "id": "infinite_btn",
      "label": "Load more button",
      "default": false,
      "visible_if": "{{ section.settings.infinite == 'infinite' }}"
    },
    {
      "type": "checkbox",
      "id": "infinite_progressbar",
      "label": "Progress bar",
      "default": false,
      "visible_if": "{{ section.settings.infinite == 'infinite' }}"
    },
    {
      "type": "header",
      "content": "t:sections.main-collection-product-grid.settings.header__3.content"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__2.label"
        },
        {
          "value": "square",
          "label": "t:sections.main-collection-product-grid.settings.image_ratio.options__3.label"
        },
        {
          "value": "landscape",
          "label": "Rectangle (4:3)"
        }
      ],
      "default": "adapt",
      "label": "t:sections.main-collection-product-grid.settings.image_ratio.label"
    },
    {
      "type": "select",
      "id": "image_shape",
      "options": [
        {
          "value": "default",
          "label": "t:sections.all.image_shape.options__1.label"
        },
        {
          "value": "arch",
          "label": "t:sections.all.image_shape.options__2.label"
        },
        {
          "value": "blob",
          "label": "t:sections.all.image_shape.options__3.label"
        },
        {
          "value": "chevronleft",
          "label": "t:sections.all.image_shape.options__4.label"
        },
        {
          "value": "chevronright",
          "label": "t:sections.all.image_shape.options__5.label"
        },
        {
          "value": "diamond",
          "label": "t:sections.all.image_shape.options__6.label"
        },
        {
          "value": "parallelogram",
          "label": "t:sections.all.image_shape.options__7.label"
        },
        {
          "value": "round",
          "label": "t:sections.all.image_shape.options__8.label"
        }
      ],
      "default": "default",
      "label": "t:sections.all.image_shape.label"
    },
    {
      "type": "checkbox",
      "id": "show_secondary_image",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.show_secondary_image.label"
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.show_vendor.label"
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "default": false,
      "label": "t:sections.main-collection-product-grid.settings.show_rating.label",
      "info": "t:sections.main-collection-product-grid.settings.show_rating.info"
    },
    {
      "type": "select",
      "id": "quick_add",
      "default": "none",
      "label": "t:sections.main-collection-product-grid.settings.quick_add.label",
      "options": [
        {
          "value": "none",
          "label": "t:sections.main-collection-product-grid.settings.quick_add.options.option_1"
        },
        {
          "value": "standard",
          "label": "t:sections.main-collection-product-grid.settings.quick_add.options.option_2"
        },
        {
          "value": "bulk",
          "label": "t:sections.main-collection-product-grid.settings.quick_add.options.option_3"
        }
      ]
    },
        {
      "type": "header",
      "content": "Product Card"
    },
    {
      "type": "select",
      "id": "badge_position",
      "label": "Badge position",
      "options": [
        { "value": "top-left", "label": "Top left" },
        { "value": "top-right", "label": "Top right" }
      ],
      "default": "top-left"
    },
    {
      "type": "range",
      "id": "card_radius",
      "min": 0,
      "max": 24,
      "step": 2,
      "unit": "px",
      "label": "Card corner radius",
      "default": 12
    },
    {
      "type": "header",
      "content": "Delivery Info"
    },
    {
      "type": "checkbox",
      "id": "show_delivery",
      "default": true,
      "label": "Show delivery info"
    },
    {
      "type": "text",
      "id": "delivery_text",
      "label": "Delivery text",
      "default": "Order now & get it by 28-29 March"
    },
    {
      "type": "header",
      "content": "Color Swatches"
    },
    {
      "type": "checkbox",
      "id": "show_swatches",
      "default": true,
      "label": "Show color swatches"
    },
    {
      "type": "text",
      "id": "swatch_option_name",
      "label": "Swatch option name",
      "default": "Color, Colour, Colours",
      "info": "Comma-separated names (e.g. Color, Colour, Colours). Also auto-detects any option containing \"color\" or \"colour\"."
    },
    {
      "type": "header",
      "content": "t:sections.main-collection-product-grid.settings.header__1.content"
    },
    {
      "type": "checkbox",
      "id": "enable_filtering",
      "default": true,
      "label": "t:sections.main-collection-product-grid.settings.enable_filtering.label",
      "info": "t:sections.main-collection-product-grid.settings.enable_filtering.info"
    },
    {
      "type": "select",
      "id": "filter_type",
      "options": [
        {
          "value": "horizontal",
          "label": "t:sections.main-collection-product-grid.settings.filter_type.options__1.label"
        },
        {
          "value": "vertical",
          "label": "t:sections.main-collection-product-grid.settings.filter_type.options__2.label"
        },
        {
          "value": "drawer",
          "label": "t:sections.main-collection-product-grid.settings.filter_type.options__3.label"
        }
      ],
      "default": "horizontal",
      "label": "t:sections.main-collection-product-grid.settings.filter_type.label"
    },
    {
      "type": "checkbox",
      "id": "enable_sorting",
      "default": true,
      "label": "t:sections.main-collection-product-grid.settings.enable_sorting.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "collection_item",
      "name": "Category",
      "settings": [
        {
          "type": "header",
          "content": "Configuration"
        },
        {
          "type": "collection",
          "id": "collection_ref",
          "label": "Select Collection"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Custom Title"
        },
        {
          "type": "image_picker",
          "id": "custom_image",
          "label": "Custom Image"
        },
        {
          "type": "image_picker",
          "id": "droplet_image",
          "label": "Droplet Icon"
        },
        {
          "type": "url",
          "id": "custom_url",
          "label": "Custom Link (Optional)"
        }
      ]
    },
    {
  "type": "block",
  "name": "Tab",
  "settings": [
    {
      "type": "text",
      "id": "label",
      "label": "Tab label"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Link"
    },
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "Show tab",
      "default": true
    }
  ]
},
{
  "type": "bmsm_promo",
  "name": "Buy More, Save More Promo",
  "settings": [
    {
      "type": "number",
      "id": "insert_after",
      "label": "Insert after product #",
      "default": 8
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Main Background Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "color_scheme",
      "id": "card_color_scheme",
      "label": "Offer Card Color Scheme",
      "default": "scheme-2"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Card Heading",
      "default": "Buy More, Save More"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Main Image"
    },
    {
      "type": "select",
      "id": "desktop_image_position",
      "options": [
        {
          "value": "right",
          "label": "Text on Left, Image on Right"
        },
        {
          "value": "left",
          "label": "Image on Left, Text on Right"
        }
      ],
      "default": "right",
      "label": "Desktop Image Position"
    },
    {
      "type": "header",
      "content": "Tier 1"
    },
    {
      "type": "text",
      "id": "tier_1_qty_text",
      "label": "Quantity Text",
      "default": "3+ Panties"
    },
    {
      "type": "text",
      "id": "tier_1_discount_text",
      "label": "Discount Text",
      "default": "5% OFF"
    },
    {
      "type": "header",
      "content": "Tier 2"
    },
    {
      "type": "text",
      "id": "tier_2_qty_text",
      "label": "Quantity Text",
      "default": "5+ Panties"
    },
    {
      "type": "text",
      "id": "tier_2_discount_text",
      "label": "Discount Text",
      "default": "10% OFF"
    },
    {
      "type": "header",
      "content": "Tier 3"
    },
    {
      "type": "text",
      "id": "tier_3_qty_text",
      "label": "Quantity Text",
      "default": "8+ Panties"
    },
    {
      "type": "text",
      "id": "tier_3_discount_text",
      "label": "Discount Text",
      "default": "15% OFF"
    },
    {
      "type": "header",
      "content": "Decorative Shape"
    },
    {
      "type": "image_picker",
      "id": "shape_image",
      "label": "Shape Image (PNG)"
    },
    {
      "type": "range",
      "id": "shape_desktop_width",
      "min": 10,
      "max": 150,
      "step": 2,
      "unit": "%",
      "label": "Desktop Width",
      "default": 40
    },
    {
      "type": "range",
      "id": "shape_desktop_top",
      "min": -50,
      "max": 100,
      "step": 2,
      "unit": "%",
      "label": "Desktop Top Position",
      "default": 10
    },
    {
      "type": "range",
      "id": "shape_desktop_left",
      "min": -50,
      "max": 100,
      "step": 2,
      "unit": "%",
      "label": "Desktop Left Position",
      "default": 10
    },
    {
      "type": "range",
      "id": "shape_mobile_width",
      "min": 10,
      "max": 150,
      "step": 2,
      "unit": "%",
      "label": "Mobile Width",
      "default": 30
    },
    {
      "type": "range",
      "id": "shape_mobile_top",
      "min": -50,
      "max": 100,
      "step": 2,
      "unit": "%",
      "label": "Mobile Top Position",
      "default": 10
    },
    {
      "type": "range",
      "id": "shape_mobile_left",
      "min": -50,
      "max": 100,
      "step": 2,
      "unit": "%",
      "label": "Mobile Left Position",
      "default": 10
    },
    {
  "type": "header",
  "content": "Top Decorative Image"
},
{
  "type": "image_picker",
  "id": "panty_top_image",
  "label": "Top Image"
},
{
  "type": "range",
  "id": "panty_top_desktop_width",
  "min": 5,
  "max": 100,
  "step": 1,
  "unit": "%",
  "label": "Desktop Width",
  "default": 25
},
{
  "type": "range",
  "id": "panty_top_desktop_top",
  "min": -20,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Desktop Position (Top)",
  "default": 5
},
{
  "type": "range",
  "id": "panty_top_desktop_left",
  "min": -20,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Desktop Position (Left)",
  "default": 5
},
{
  "type": "range",
  "id": "panty_top_mobile_width",
  "min": 5,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Mobile Width",
  "default": 25
},
{
  "type": "range",
  "id": "panty_top_mobile_top",
  "min": -20,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Mobile Position (Top)",
  "default": 5
},
{
  "type": "range",
  "id": "panty_top_mobile_left",
  "min": -20,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Mobile Position (Left)",
  "default": 5
},
{
  "type": "header",
  "content": "Bottom Decorative Image"
},
{
  "type": "image_picker",
  "id": "panty_bottom_image",
  "label": "Bottom Image"
},
{
  "type": "range",
  "id": "panty_bottom_desktop_width",
  "min": 5,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Desktop Width",
  "default": 25
},
{
  "type": "range",
  "id": "panty_bottom_desktop_bottom",
  "min": -20,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Desktop Position (Bottom)",
  "default": 5
},
{
  "type": "range",
  "id": "panty_bottom_desktop_right",
  "min": -20,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Desktop Position (Right)",
  "default": 5
},
{
  "type": "range",
  "id": "panty_bottom_mobile_width",
  "min": 5,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Mobile Width",
  "default": 25
},
{
  "type": "range",
  "id": "panty_bottom_mobile_bottom",
  "min": -20,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Mobile Position (Bottom)",
  "default": 5
},
{
  "type": "range",
  "id": "panty_bottom_mobile_right",
  "min": -20,
  "max": 50,
  "step": 1,
  "unit": "%",
  "label": "Mobile Position (Right)",
  "default": 5
},
    {
      "type": "header",
      "content": "Block Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 36
    }
  ]
},
   {
  "type": "feature_promo",
  "name": "Feature Promo Card",
  "settings": [
    {
      "type": "number",
      "id": "insert_after",
      "label": "Insert after product #",
      "default": 8
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Discreet. Waterproof. Always with you."
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Text",
      "default": "The Sereluna waterproof bag keeps used period underwear safely stored when you're on the go — no leaks, no worries."
    },
    {
      "type": "text",
      "id": "feature_1",
      "label": "Feature 1",
      "default": "100% waterproof & leak-proof"
    },
    {
      "type": "text",
      "id": "feature_2",
      "label": "Feature 2",
      "default": "Hygienic & easy to clean"
    },
    {
      "type": "text",
      "id": "feature_3",
      "label": "Feature 3",
      "default": "Perfect for work, school & travel"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button Label",
      "default": "Shop Waterproof Bag"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Product Image"
    },
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "Mobile Background Image",
      "info": "If left blank, the desktop image will be used."
    },
  ]
}
  ],
}
{% endschema %}
